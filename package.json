{
  "name": "ruma",
  "type": "module",
  "devDependencies": {
    "@types/bun": "latest",
    "husky": "^9.1.7",
    "lint-staged": "^15.2.11"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "scripts": {
    "postinstall": "bun install --cwd client; bun install --cwd anchor",
    "prepare": "husky"
  }
}
